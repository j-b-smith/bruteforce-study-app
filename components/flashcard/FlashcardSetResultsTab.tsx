import { Box, Text } from "@chakra-ui/react";

const FlashcardSetResultsTab = () => {
  return (
    <Box p={4}>
      <Text>Results for studied flashcard sets will be displayed here.</Text>
    </Box>
  );
};

export default FlashcardSetResultsTab;
